- provide :title, t(".title")
= simple_form_for [:admin, @high_transfer], html: {id: "admin_high_transfer"} do |f|
  = render "form", f: f, support: @support
  .col-lg-12.text-center
    = f.submit  t(".submit"), class: 'btn btn-primary'
